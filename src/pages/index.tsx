import TokenSwap from '../components/TokenSwap';
import '../app/globals.css'

const Home = () => (
  <div className="min-h-screen flex items-center justify-center home-screen">
    <TokenSwap />
  </div>
);

export default Home;
